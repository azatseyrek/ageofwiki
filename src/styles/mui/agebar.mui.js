import {makeStyles} from '@material-ui/core';

export const agebarStyle = makeStyles({
  root: {
    flexGrow: 1,
  },
  tabLabel: {
    fontSize: 18,
    fontFamily: 'MedievalSharp, sans',
    color: 'white',
  },
});
